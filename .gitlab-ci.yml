test:
  stage: test
  image: python:3.11
  coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'
  script:
    - pip install poetry
    - poetry install
    - poetry run coverage run -m tox
    - poetry run coverage report
  tags:
    - docker